<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://pablomirallesg.com/feed.xml" rel="self" type="application/atom+xml" /><link href="https://pablomirallesg.com/" rel="alternate" type="text/html" /><updated>2025-12-11T12:01:13+01:00</updated><id>https://pablomirallesg.com/feed.xml</id><title type="html">Pablo Miralles</title><subtitle>Personal website of Pablo Miralles, Mathematics and Computer Science graduate, PhD student in Deep Learning at Technical University of Madrid</subtitle><entry><title type="html">Coding a fused matrix multiplication and log-sum-exp reduction in Triton to retrieve LLM log-probabilities faster and with lower memory overhead</title><link href="https://pablomirallesg.com/blog/fused-matmul-logsumexp" rel="alternate" type="text/html" title="Coding a fused matrix multiplication and log-sum-exp reduction in Triton to retrieve LLM log-probabilities faster and with lower memory overhead" /><published>2025-12-11T00:00:00+01:00</published><updated>2025-12-11T00:00:00+01:00</updated><id>https://pablomirallesg.com/blog/fused-matmul-logsumexp</id><content type="html" xml:base="https://pablomirallesg.com/blog/fused-matmul-logsumexp"><![CDATA[<h3 id="motivation">Motivation</h3>
<p>In my research I have worked with methods that involve retrieving the log-probabilities of an input text from a large language model. Let me first introduce what I mean exactly and why this is useful.</p>

<p>Consider an input text tokenized as a sequence $t_1,\dots,t_L$. Each token is mapped to an initial embedding, forming the initial hidden-state matrix $\mathrm{HS}^0 \in \mathbb{R}^{L \times D}$. The transformer then processes these representations and produces the contextualized final hidden states</p>

\[f(\mathrm{HS}^0;\theta) = \mathrm{HS}^{\text{final}} \in \mathbb{R}^{L \times D}.\]

<p>A final linear layer with weights $W \in \mathbb{R}^{V \times D}$ and bias $b \in \mathbb{R}^{V}$ is applied to obtain the logits</p>

\[\mathrm{logits} = \mathrm{HS}^{\text{final}}\cdot W^T + b \in \mathbb{R}^{L \times V}.\]

<p>Applying the softmax along the vocabulary dimension yields the probability matrix</p>

\[P = \mathrm{softmax}(\mathrm{logits},\ \mathrm{dim}=1) \in \mathbb{R}^{L \times V}.\]

<p>This probability matrix encodes the model’s next-token predictions. For each position $i \in {1,\dots,L}$, the vector $P_i \in \mathbb{R}^{V}$ is a valid probability distribution (non-negative and summing to one) over the vocabulary. Its $v$-th component represents the model’s estimated probability that the next token is the $v$-th vocabulary item, conditioned on the preceding context $t_1,\dots,t_i$.</p>

<p>During the generation process we only compute the final probability vector to sample the new token to be appended. However, we can apply the model to a full input text and study these probabilities across tokens. This is useful in distinguishing AI-generated text (we expect these texts to score higher when passed through the LLM), and we have also leveraged them for human authorship problems.</p>

<h3 id="computational-problems">Computational problems</h3>
<p>The vocabulary of modern LLMs is very large (100K–300K tokens), and the matrix $P \in \mathbb{R}^{L \times V}$ becomes extremely large for long texts, especially in batched settings. Often we only need the log-probability of the token that actually occurred next in the sequence; i.e., we want the sequence</p>

\[\log P_{1, t_2},\ \log P_{2, t_3},\ \dots,\ \log P_{L-1, t_L}.\]

<p>If we compute these values smartly, we avoid instantiating the full probability or logit matrix in memory!</p>

<h3 id="derivations-and-algorithm">Derivations and algorithm</h3>

<p>Now, we can consider the following equivalence:</p>

\[\log P
= \mathrm{logsoftmax}(\mathrm{logits},\ \mathrm{dim}=1)
= \mathrm{logits} - \mathrm{logsumexp}(\mathrm{logits},\ \mathrm{dim}=1),\]

<p>and therefore</p>

\[\log P_{i, t_{i+1}}
= \mathrm{logits}_{i, t_{i+1}} - \mathrm{logsumexp}(\mathrm{logits}_i),\]

<p>The first component is actually very easy to simplify:</p>

\[\mathrm{logits}_{i, t_{i+1}} =
\mathrm{HS}^{\text{final}}_i \cdot W_{t_{i+1}} + b_{t_{i+1}}.\]

<p>The second term is a reduction across the vocabulary dimension. In other words, $\mathrm{logsumexp}(\mathrm{logits},\ \mathrm{dim}=1)$ yields a length-$L$ vector, not a massive matrix. Naively, you’d instantiate the full logits to compute <code class="language-plaintext highlighter-rouge">logsumexp</code>, which is expensive. Instead, we can fuse the linear layer that produces logits with the <code class="language-plaintext highlighter-rouge">logsumexp</code> reduction in a single kernel.</p>

<h3 id="tiled-matmul--online-log-sum-exp">Tiled matmul &amp; online log-sum-exp</h3>
<p>Consider an input matrix $A\in \mathbb R^{M \times K}$, a weight matrix $B\in \mathbb R^{N \times K}$ (we ignore the bias vector for simplicity here). We want to implement the following:</p>

\[C = \mathrm{logsumexp} (A \cdot B^T, \mathrm{dim} = 1),\]

<p>or, in non-vectorial form:</p>

\[C_i = \log \sum_{j=1}^N \exp(A_i \cdot B_j^T); \qquad i=1,\dots M.\]

<p>As with general matrix multiplication, we tile the matrices; each tile pair is handled by a tensor core. While computing tile products, we apply the exponential and maintain the running sum of exponentials across the $N$ dimension. After accumulation along $N$, we take the logarithm.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">BLOCK_SIZE_M</span><span class="p">):</span>
    <span class="n">reduce_global</span> <span class="o">=</span> <span class="n">zeros</span><span class="p">((</span><span class="n">BLOCK_SIZE_M</span><span class="p">,),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">float32</span><span class="p">)</span>
    
    <span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">BLOCK_SIZE_N</span><span class="p">):</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">zeros</span><span class="p">((</span><span class="n">BLOCK_SIZE_M</span><span class="p">,</span> <span class="n">BLOCK_SIZE_N</span><span class="p">),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">float32</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">BLOCK_SIZE_K</span><span class="p">):</span>
            <span class="n">a</span> <span class="o">=</span> <span class="n">A</span><span class="p">[</span><span class="n">m</span> <span class="p">:</span> <span class="n">m</span><span class="o">+</span><span class="n">BLOCK_SIZE_M</span><span class="p">,</span> <span class="n">k</span> <span class="p">:</span> <span class="n">k</span><span class="o">+</span><span class="n">BLOCK_SIZE_K</span><span class="p">]</span>
            <span class="n">b</span> <span class="o">=</span> <span class="n">B</span><span class="p">[</span><span class="n">n</span> <span class="p">:</span> <span class="n">n</span><span class="o">+</span><span class="n">BLOCK_SIZE_N</span><span class="p">,</span> <span class="n">k</span> <span class="p">:</span> <span class="n">k</span><span class="o">+</span><span class="n">BLOCK_SIZE_K</span><span class="p">]</span>
            <span class="n">acc</span> <span class="o">+=</span> <span class="n">dot</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">.</span><span class="n">T</span><span class="p">)</span>

        <span class="n">reduce_global</span> <span class="o">+=</span> <span class="nb">sum</span><span class="p">(</span><span class="n">exp</span><span class="p">(</span><span class="n">acc</span><span class="p">),</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    
    <span class="n">C</span> <span class="o">=</span> <span class="n">log</span><span class="p">(</span><span class="n">reduce_global</span><span class="p">)</span>
</code></pre></div></div>

<p>After dividing the algorithm in blocks, we can parallelize the program across tensor cores by blocks. Since we have a reduction dependency in both the $N$ and $K$ dimensions, we will parallelize across blocks in the $M$ dimension. However, there is an additional implementation detail that is typically implemented for numerical stability, which is mathematically equivalent but avoids computing the exponential of large numbers:</p>

\[\begin{align}
M &amp;= \max (A \cdot B^T,\ \mathrm{dim}=1) \\
C &amp;= M + \mathrm{logsumexp} (A \cdot B^T - M,\ \mathrm{dim} = 1).
\end{align}\]

<p>This slightly complicates computation across $N$-tiles: we must track the online maximum and the sum of exponentials with the maximum subtracted. The algorithm:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">BLOCK_SIZE_M</span><span class="p">):</span>
    <span class="n">reduce_global</span> <span class="o">=</span> <span class="n">zeros</span><span class="p">((</span><span class="n">BLOCK_SIZE_M</span><span class="p">,),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">float32</span><span class="p">)</span>
    <span class="n">max_global</span> <span class="o">=</span> <span class="n">full</span><span class="p">((</span><span class="n">BLOCK_SIZE_M</span><span class="p">,),</span> <span class="nb">float</span><span class="p">(</span><span class="s">'-inf'</span><span class="p">),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">float32</span><span class="p">)</span>
    
    <span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">BLOCK_SIZE_N</span><span class="p">):</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">zeros</span><span class="p">((</span><span class="n">BLOCK_SIZE_M</span><span class="p">,</span> <span class="n">BLOCK_SIZE_N</span><span class="p">),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">float32</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">BLOCK_SIZE_K</span><span class="p">):</span>
            <span class="n">a</span> <span class="o">=</span> <span class="n">A</span><span class="p">[</span><span class="n">m</span> <span class="p">:</span> <span class="n">m</span><span class="o">+</span><span class="n">BLOCK_SIZE_M</span><span class="p">,</span> <span class="n">k</span> <span class="p">:</span> <span class="n">k</span><span class="o">+</span><span class="n">BLOCK_SIZE_K</span><span class="p">]</span>
            <span class="n">b</span> <span class="o">=</span> <span class="n">B</span><span class="p">[</span><span class="n">n</span> <span class="p">:</span> <span class="n">n</span><span class="o">+</span><span class="n">BLOCK_SIZE_N</span><span class="p">,</span> <span class="n">k</span> <span class="p">:</span> <span class="n">k</span><span class="o">+</span><span class="n">BLOCK_SIZE_K</span><span class="p">]</span>
            <span class="n">acc</span> <span class="o">+=</span> <span class="n">dot</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">.</span><span class="n">T</span><span class="p">)</span>

        <span class="n">max_block</span> <span class="o">=</span> <span class="nb">max</span><span class="p">(</span><span class="n">acc</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">exp</span><span class="p">(</span><span class="n">acc</span> <span class="o">-</span> <span class="n">max_block</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">])</span>
        <span class="n">reduce_block</span> <span class="o">=</span> <span class="nb">sum</span><span class="p">(</span><span class="n">acc</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>

        <span class="c1"># Update global values
</span>        <span class="n">max_global_new</span> <span class="o">=</span> <span class="n">maximum</span><span class="p">(</span><span class="n">max_global</span><span class="p">,</span> <span class="n">max_block</span><span class="p">)</span>
        <span class="n">reduce_global</span> <span class="o">=</span> <span class="p">(</span>
            <span class="n">reduce_global</span> <span class="o">*</span> <span class="n">exp</span><span class="p">(</span><span class="n">max_global</span> <span class="o">-</span> <span class="n">max_global_new</span><span class="p">)</span> <span class="o">+</span>
            <span class="n">reduce_block</span> <span class="o">*</span> <span class="n">exp</span><span class="p">(</span><span class="n">max_block</span> <span class="o">-</span> <span class="n">max_global_new</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">max_global</span> <span class="o">=</span> <span class="n">max_global_new</span>
    
    <span class="n">C</span> <span class="o">=</span> <span class="n">max_global</span> <span class="o">+</span> <span class="n">log</span><span class="p">(</span><span class="n">reduce_global</span><span class="p">)</span>
</code></pre></div></div>

<p>The full code is available at <a href="https://github.com/pablomiralles22/fused-matmul-logsumexp">pablomiralles22/fused-matmul-logsumexp</a>, and you will find a good description and instructions on how to use it on the <code class="language-plaintext highlighter-rouge">README</code> file.</p>

<h3 id="benchmarking">Benchmarking</h3>
<p>Consider an input matrix $X\in \mathbb R^{M \times K}$, a weight matrix $W\in \mathbb R^{N \times K}$, and a bias vector $b\in \mathbb R^{N}$. The following plots show execution time and peak memory for varying $M$ and $N$, comparing the fused implementation to a naive baseline:</p>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_N-480.webp 480w,/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_N-800.webp 800w,/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_N-1400.webp 1400w," type="image/webp" sizes="95vw" />
    
    <img src="/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_N.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_M-480.webp 480w,/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_M-800.webp 800w,/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_M-1400.webp 1400w," type="image/webp" sizes="95vw" />
    
    <img src="/assets/img/posts/2025-12-11-fused-matmul-logsumexp/benchmark_vs_M.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>]]></content><author><name></name></author><category term="deep-learning" /><category term="triton" /><summary type="html"><![CDATA[Fusing matmul + log-sum-exp in Triton to read LLM token log-probabilities efficiently (less memory, faster)]]></summary></entry><entry><title type="html">Running LLMs in low-resource settings</title><link href="https://pablomirallesg.com/blog/llm-efficiency" rel="alternate" type="text/html" title="Running LLMs in low-resource settings" /><published>2025-11-19T00:00:00+01:00</published><updated>2025-11-19T00:00:00+01:00</updated><id>https://pablomirallesg.com/blog/llm-efficiency</id><content type="html" xml:base="https://pablomirallesg.com/blog/llm-efficiency"><![CDATA[<p>In this post I try to create an index of the main ideas I have found to be able to run Large Language Models in my low-resource servers, and to run them as efficiently as possible. The point is not to explain every technique in-depth, but to provide an index with high-level descriptions. I have decided to divide them in three categories: <strong>general</strong>, <strong>training-specific</strong> and <strong>inference-specific</strong> optimizations.</p>

<h2 id="general">General</h2>
<h4 id="flashattention">FlashAttention</h4>
<p>FlashAttention is an algorithm to compute</p>

\[\mathrm{Attention}(K, Q, V) = \mathrm{softmax}\left( \frac{QK^T}{\sqrt{d_k}} \right)V\]

<p>efficiently in GPUs. As you probably know, this formula is quadratic in the input sequence length, making it problematic with very long sequences. FlashAttention remains quadratic in execution time, but avoids materializing the full $N \times N$ attention matrix in memory by computing the result in small tiles of $Q$, $K$ and $V$, and recomputing the result on the backward pass for training instead of storing the attention matrix. New versions with additional optimizations are available, but I think these are the most important one. We do not need to worry about setting this algorithm because all modern models use FlashAttention by default.</p>

<h4 id="mixture-of-experts-moe">Mixture-of-experts (MoE)</h4>
<p>A mixture-of-experts (MoE) model splits a neural network into many specialized <em>experts</em>, and uses a <strong>gating network</strong> to decide which experts handle each input. In the case of LLMs, this is typically applied in the feed-forward layers. Instead of scaling the MLP layers by widening them, the network uses several MLP experts and routes each token to a few of them (usually 1-2). This scales the number of effective parameters without increasing inference costs too much.</p>

<p>As a side note, during training we must make sure that the tokens are spread across experts to avoid collapsing to a subset of experts. This can be done via an auxiliary load-balancing loss term, adding noise to the gating network that routes token, or other ideas.</p>

<h4 id="datamodel-parallelism">Data/model parallelism</h4>
<p>The concept of parallelism is key both to speed up execution and to be able to run large models that do not fit in a single GPU.</p>

<p>The simplest type is <strong>data parallelism</strong>, where we store a full copy of the model in each GPU, and the data is sharded across GPUs and processed independently. For training it is then necessary to sync the gradients in all devices before performing the backward pass. This option is great to speed up execution, but it requires being able to fit the model in each device.</p>

<p>In <strong>model parallelism</strong>, each device holds only <em>part</em> of the model, and the execution of the model is orchestrated across them. We can distinguish two types:</p>
<ul>
  <li><strong>Tensor parallelism</strong>. Each layer of the model is split across the devices. For example, the columns or rows in the weight matrix of a linear layer could be divided by GPU, and the result synced and concatenated between devices.</li>
  <li><strong>Pipeline parallelism</strong>. The layers are grouped in stages and divided into the devices. For example, the first device could execute layers 1 to 4, and the second device layers 5 to 8. Of course, there is a dependency between the devices, both in the forward and in the backward pass. To avoid idle time, the batch is divided in micro-batches and the execution segmented across them. For example, the first device could execute the first micro-batch, and pass the result to the second device. While the second device processes the first micro-batch, the first device would start processing the second micro-batch.</li>
</ul>

<p>There are several advantages and disadvantages for both options. Tensor parallelism enables very big layers to be run and doesn’t need micro-batching, but it needs a lot of communication between devices, requiring high-bandwidth/low-latency interconnects (e.g. NVLink). On the other hand, pipeline parallelism needs minimal communication between devices, but requires micro-batching and balanced stages to avoid idleness.</p>

<p>All types of parallelism discussed thus far are depicted in the following figure:</p>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2025-11-19-llm-efficiency/parallelism.drawio.svg" sizes="95vw" />
    
    <img src="/assets/img/posts/2025-11-19-llm-efficiency/parallelism.drawio.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

<p>One final note is that all of these types of parallelism can be combined. For example, we could have two copies of the model, each one using three devices via pipeline parallelism, requiring six GPUs in total.</p>

<p>Regarding the implementation, for inference I recommend the <code class="language-plaintext highlighter-rouge">vLLM</code> library, which allows the use of all types of parallelism via simple command line arguments. Training is a bit more complex. The <code class="language-plaintext highlighter-rouge">transformers</code> library offers data parallelism, as well as the <code class="language-plaintext highlighter-rouge">device_map="auto"</code> option when loading the model, which implements some rudimentary pipeline parallelism without segmentation (thus underutilizing GPUs greatly). For a more optimized or complete optimization, we would have to use something like the <code class="language-plaintext highlighter-rouge">deepspeed</code> library.</p>

<h4 id="lower-precision-mixed-precision-and-quantization">Lower precision, mixed precision and quantization</h4>
<p>Most operations can be run in 16-bit precision (FP16 or BF16) without problems, allowing us to reduce memory usage by a factor of 2, and speeding up computations a lot with modern hardware that is adapted to use this precision.</p>

<p>Some operations like reductions/accumulations or normalizations are better performed in FP32 to avoid overflow/underflow or to reduce rounding errors. Thus, it is common to keep these operations in 32-bit precision (<strong>mixed precision</strong>). During training, it is also common to keep master weights of the model or optimizer states in FP32 as well.</p>

<p>A step further is <strong>quantization</strong>, representing model parameters and sometimes activations with integers (e.g., int8, int4) instead of floating point (FP32/BF16/FP16). The goal is to reduce memory footprint and improve inference speed, especially on hardware that has fast integer arithmetic. The trick here is performing this compression of the model without losing too much accuracy. Very often, we find versions of the top open-source models already quantized with modern techniques like AWQ, so we do not need to worry about it.</p>

<h4 id="cpu-offloading">CPU offloading</h4>
<p>If you are very short on memory but absolutely need to run a large model, you can offload some of the weights to CPU, and swap them around with the GPU to complete all computations. However, I highly recommend against it because it is extremely slow.</p>

<h2 id="training-specific">Training-specific</h2>
<h4 id="parameter-efficient-fine-tuning-lora-and-soft-prompting">Parameter efficient fine-tuning: LoRA and soft prompting</h4>
<p>In language modeling, we typically start from a pre-trained model that is then fine-tuned in our downstream task of interest. In most cases, it is sufficient to perform a “reduced” update to the network instead of updating all parameters, greatly reducing memory requirements for the gradients and optimizer states. What is more, given the small amount of labeled data we often have and the high dimensionality of textual data, these reduced updates are more likely to generalize better.</p>

<p>The most common example is <strong>LoRA (Low-Rank Adaptation)</strong>, where a matrix $W \in \mathbb R^{n_{in} \times n_{out}}$ is updated with a low rank matrix $W_{new} = W + AB$ with trainable matrices $A \in \mathbb R^{n_{in} \times r}$ and  $B \in \mathbb R ^{r \times n_{out}}$ and a small rank $r$. Another example is the concept of <strong>soft prompting</strong>, where a few trainable embeddings are prepended to all input sequences.</p>

<h4 id="qlora-quantized-low-rank-adaptation">QLoRA (Quantized Low-Rank Adaptation)</h4>
<p>The next step in LoRA is to quantize the base model while training non-quantized low rank adapters, achieving very good compression without sacrificing a lot of performance.</p>

<h4 id="optimizers-with-smaller-states">Optimizers with smaller states</h4>
<p>The Adam optimizer uses $2N$ values for the momentum and second moment running estimates, where $N$ is the number of trainable parameters in the model. We can reduce it to $N$ by switching to Nesterov or RMSProp, or reduce it event further to $\approx\sqrt{N}$ with Adafactor. The tradeoff in all cases is reducing training stability and convergence speed.</p>

<h4 id="accumulate-gradient-batches">Accumulate gradient batches</h4>
<p>Instead of using a large batch size, we can instead use a small batch size and accumulate the gradient for multiple batches before performing an optimization step. This is equivalent thanks to the linearity of the gradient operator, except in models that include batch level operations (e.g. batch normalization), which is rare in language models. With this, we trade-off training speed to reduce memory usage, which is often the stricter limiting factor.</p>

<h4 id="gradient-checkpointing">Gradient checkpointing</h4>
<p>During the forward pass, it is common to store values that will be used in the backward to avoid wasteful recomputation. However, this is undesirable when the bottleneck is the GPU memory available. Disabling this is called <strong>gradient checkpointing</strong>.</p>

<h4 id="gradientoptimizer-state-sharding">Gradient/optimizer state sharding</h4>
<p>While training in a data parallel setting, each GPU contains a copy of the gradients and optimizer states for all parameters. However, this is a bit wasteful, and we can shard (divide) these vectors across GPUs at the expense of some communication overhead.</p>

<h2 id="inference-specific">Inference-specific</h2>
<h4 id="kv-cache">KV Cache</h4>
<p>Autoregressive models use a causal attention mask that blocks each token from attending to future tokens. As a result, when generating text one token at a time, newly produced tokens don’t influence the earlier ones, only the other way around. This means we don’t need to redo computations for previous tokens. However, the new token still depends on earlier tokens through their attention keys and values. To avoid recomputing these for every step, we store the keys and values of all previously generated tokens in a <strong>KV cache</strong>. During each new forward pass, the model retrieves these cached representations and only performs computations for the new token, greatly speeding up generation. Regarding the implementation, this is already done in the typical generation libraries (e.g. <code class="language-plaintext highlighter-rouge">HuggingFace</code> or <code class="language-plaintext highlighter-rouge">vLLM</code>).</p>

<h4 id="paged-attention">Paged attention</h4>
<p><strong>Paged attention</strong> implements a type of paged virtual memory for the KV cache in the GPU RAM, removing the need for large contiguous blocks for long sequences and improving memory utilization. This is implemented in <code class="language-plaintext highlighter-rouge">vLLM</code>.</p>

<h4 id="speculative-decoding-and-draft-models">Speculative decoding and draft models</h4>
<p>Speculative decoding pairs a large language model with a smaller, faster “draft model”. The draft model predicts several upcoming tokens, and the large model then checks those predictions in parallel, accepting the ones that match what it would have produced itself and discarding the rest. This allows the system to generate multiple tokens per verification step instead of one at a time, significantly reducing latency while preserving the large model’s output quality.</p>

<h4 id="efficient-batching-and-scheduling">Efficient batching and scheduling</h4>
<p><code class="language-plaintext highlighter-rouge">vLLM</code> implements and efficient scheduling algorithm where new requests made to the model are dynamically added to the current batch, and completed generations are dynamically removed from the batch as well, improving resource utilization.</p>

<h4 id="lora-for-multi-model-serving">LoRA for multi-model serving</h4>
<p>When serving the same base model with several LoRA adapters on top, you can share the common base in GPU memory and enable or disable the pertinent adapters at runtime, making it very efficient to deploy. This is also implemented in <code class="language-plaintext highlighter-rouge">vLLM</code>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[A small index of all ideas I have come to learn to run LLM efficiently, and with low resources.]]></summary></entry><entry><title type="html">Multivariate Hawkes process with Jax</title><link href="https://pablomirallesg.com/blog/hawkes-multivariate" rel="alternate" type="text/html" title="Multivariate Hawkes process with Jax" /><published>2025-11-09T00:00:00+01:00</published><updated>2025-11-09T00:00:00+01:00</updated><id>https://pablomirallesg.com/blog/hawkes-multivariate</id><content type="html" xml:base="https://pablomirallesg.com/blog/hawkes-multivariate"><![CDATA[<p>Recently, a colleague was giving a seminar on his ongoing work, and he was looking for an efficient implementation to train a multivariate Hawkes process. I though that it was a cool project to tackle with deep learning frameworks!</p>
<h2 id="definition">Definition</h2>
<p>A <strong>multivariate Hawkes process</strong> is a <em>self-exciting point process</em> that models how multiple types of events influence each other over time. Formally, we have $K$ event types, with baseline rates $\mu_i$, excitation matrix $\alpha_{ij}$, and exponential decay rate $\beta_{ij}$ (often shared as a single $\beta$). Each event type has a <strong>conditional intensity</strong> function:</p>

\[\lambda_i(t) = \mu_i + \sum_{j=1}^{K} \sum_{t_k^{(j)} &lt; t} \alpha_{ij} e^{-\beta_{ij} (t - t_k^{(j)})}\]

<p>where $t_k^{(j)}$  are the times of past events of type j.
The intuition is that each past event of type $j$ adds an <strong>exponential bump</strong> to $\lambda_i(t)$ that:</p>
<ul>
  <li>starts at height $\alpha_{ij}&gt;0$ immediately after the event,</li>
  <li>decays continuously at rate $\beta_{ij}$,</li>
  <li>and is added on top of the baseline $\mu_i$.</li>
</ul>

<p>The decay function can be substituted by any other function with similar properties, but in this implementation we assume the exponential function for convenience, as well as a single $\beta$ decay parameter. We also consider a low rank decomposition of the excitation matrix $\alpha_{ij} = u_i^T v_j$, or even $u=v$ if we assume symmetry.</p>

<h2 id="training-with-maximum-likelihood-estimation">Training with maximum likelihood estimation</h2>
<p>For observed events  ${(t_m, c_m)}_{m=1}^{N}$, where $t_m$ is the event time, $c_m \in {1,\dots,K}$ is the event type and $T$ is the observation horizon, the <strong>log-likelihood</strong> of a multivariate Hawkes process is:</p>

\[\log \mathcal{L} = \sum_{m=1}^{N} \log \lambda_{c_m}(t_m) - \sum_{i=1}^{K} \int_0^{T} \lambda_i(t) , dt\]

<p>With the exponential kernel the intensity functions is</p>

\[\lambda_i(t) = \mu_i + \sum_{j=1}^K \sum_{t_k^{(j)} &lt; t} \alpha_{ij} e^{-\beta (t - t_k^{(j)})},\]

<p>and in this case the integral term has a <strong>closed form</strong>, avoiding numerical integration:</p>

\[\int_0^T \lambda_i(t) dt = \mu_i T + \sum_{j=1}^K \sum_{t_k^{(j)} &lt; T} \frac{\alpha_{ij}}{\beta} \left(1 - e^{-\beta (T - t_k^{(j)})}\right).\]

<p>To train this model, you compute the log-likelihood above, and then maximize it (or equivalently, minimize the negative log-likelihood) with respect to the parameters:</p>

\[\Theta = { \mu_i, \alpha_{ij}, \beta }\]

<p>This can be done with any deep learning framework. We will be using Jax in this case instead of PyTorch, because I want to learn it and because it implements fast parallel scans out of the box.</p>

<h2 id="derivations">Derivations</h2>
<p>We now consider the log-likelihood and try to make some derivations that make the vectorized implementation with Jax easy.</p>

\[\log \mathcal{L} = \underbrace{\sum_{m=1}^{N} \log \lambda_{c_n}(t_n)}_{\text{A: log-likelihood of observed events}} - \underbrace{\sum_{i=1}^{K} \int_0^{T} \lambda_i(t) \, dt}_{\text{B: integral over intensities}}\]

<p><strong>Derivation of the second term</strong></p>

<p>Let’s consider the second term. First, since we have the exponential kernel:</p>

\[\int_0^T \lambda_i(t) dt = \mu_i T + \sum_{j=1}^K \sum_{t_k^{(j)} &lt; T} \frac{\alpha_{ij}}{\beta} \left(1 - e^{-\beta (T - t_k^{(j)})}\right).\]

<p>Therefore, the second term becomes (considering $\alpha_{ij}=u_i^T v_j$):</p>

\[\begin{align*} B &amp;= T \cdot \sum_{i=1}^K \mu_i + \sum_{i=1}^K \sum_{j=1}^K \sum_{t_k^{(j)}&lt;T} \frac{\alpha_{ij}}{\beta}\!\left(1-e^{-\beta\,(T-t_k^{(j)})}\right) \\ &amp;= T \cdot \sum_{i=1}^K \mu_i + \frac{1}{\beta} \sum_{i=1}^K u_i^T \left( \sum_{j=1}^K v_j \left[ \sum_{c_n=j} (1-e^{-\beta\,(T-t_n)}) \right] \right) \\ &amp;= T \cdot \sum_{i=1}^K \mu_i + \frac{1}{\beta} \left(\sum_{i=1}^K u_i^T\right) \left( \sum_{n=1}^N v_{c_n} (1-e^{-\beta\,(T-t_n)})\right) \end{align*}\]

<p>This is simple to vectorize:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">v_mark</span> <span class="o">=</span> <span class="n">v</span><span class="p">[</span><span class="n">marks</span><span class="p">]</span>  <span class="c1"># (N, R)
</span><span class="n">summed_u</span> <span class="o">=</span> <span class="n">jnp</span><span class="p">.</span><span class="nb">sum</span><span class="p">(</span><span class="n">u</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>  <span class="c1"># (R,)
</span><span class="n">exp_term</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="n">jnp</span><span class="p">.</span><span class="n">exp</span><span class="p">(</span><span class="o">-</span><span class="n">beta</span> <span class="o">*</span> <span class="n">jnp</span><span class="p">.</span><span class="n">clip</span><span class="p">(</span><span class="n">T</span> <span class="o">-</span> <span class="n">times</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="bp">None</span><span class="p">))</span>  <span class="c1"># (N,)
</span><span class="n">summed_v_mark</span> <span class="o">=</span> <span class="n">jnp</span><span class="p">.</span><span class="nb">sum</span><span class="p">(</span><span class="n">v_mark</span> <span class="o">*</span> <span class="n">exp_term</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">],</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>  <span class="c1"># (R,)
</span><span class="n">B</span> <span class="o">=</span> <span class="n">jnp</span><span class="p">.</span><span class="nb">sum</span><span class="p">(</span><span class="n">mu</span><span class="p">)</span> <span class="o">*</span> <span class="n">T</span> <span class="o">+</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">/</span> <span class="n">beta</span><span class="p">)</span> <span class="o">*</span> <span class="n">jnp</span><span class="p">.</span><span class="n">dot</span><span class="p">(</span><span class="n">summed_u</span><span class="p">,</span> <span class="n">summed_v_mark</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>Derivation of the first term and naive scan</strong></p>

<p>The first term is a bit more difficult. Let’s consider each $\lambda_{c_n} (t_n)$, and assume that the times $(t_n)_n$ are sorted:</p>

\[\begin{align*} \lambda_{c_n}(t_n) &amp;= \mu_{c_n} + \sum_{j=1}^K \sum_{\substack{t_m&lt;t_n \\ c_m=j}} u_{c_n}^T v_j \exp(-\beta(t_n-t_m)) \\ &amp;= \mu_{c_n} + \sum_{m=1}^{n-1} u_{c_n}^T v_{c_m} \cdot \exp (-\beta(t_n-t_m)) \\ &amp;= \mu_{c_n} + u_{c_n}^T \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta(t_n-t_m)) \end{align*}\]

<p>Now, can we parallelize this computation for all n? We can do the following next step:</p>

\[\begin{align*} \lambda_{c_n}(t_n) &amp;= \mu_{c_n} + u_{c_n}^T \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta(t_n-t_m)) \\ &amp;= \mu_{c_n} + \left( u_{c_n}^T \cdot \exp(-\beta t_n)\right) \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (\beta t_m). \end{align*}\]

<p>The sum term becomes a <code class="language-plaintext highlighter-rouge">cumsum</code> operation, which is a simple scan already implemented in many frameworks. However, the term $\exp(\beta t_m)$ can become very large, and this implementation could become numerically unstable.</p>

<p><strong>Non-naive scan</strong></p>

<p>Let’s consider the quantity</p>

\[S_n = \sum_{m=1}^{n} v_{c_m} \cdot \exp (-\beta(t_n-t_m)),\]

<p>Then:</p>

\[\begin{align*} S_n &amp;= v_{c_n} + \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta(t_n-t_m)) \\ &amp;= v_{c_n} + \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta((t_n-t_{n-1}) + (t_{n-1}-t_m))) \\ &amp;= v_{c_n} + \exp(-\beta(t_n-t_{n-1}))\sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta(t_{n-1}-t_m)) \\ &amp;= v_{c_n} + \exp(-\beta(t_n-t_{n-1})) \cdot S_{n-1} .\end{align*}\]

<p>This can be seen as a reduction for the operator</p>

\[\begin{array}{rcl} *: &amp; (\mathbb{R}^d \times \mathbb R) \times (\mathbb{R}^d \times \mathbb R) &amp; \longrightarrow \mathbb{R}^d \times \mathbb R \\[3pt] &amp; (v_1, t_1),(v_2,t_2) &amp; \longmapsto \left( v_2 + \exp(-\beta(t_2-t_1)) \cdot v_1 , t_2\right), \end{array}\]

<p>which is associative, and this can be implemented as a parallel scan, like the cumulative sum. The difference now is that since the timestamps are ordered, the quantities $\exp(-\beta (t_2-t_1))$ with $t_2&gt;t_1$ are always less than $1$, and they are bounded and controlled. We can now derive the final implementation:</p>

\[\begin{align*} \lambda_{c_n}(t_n) &amp;= \mu_{c_n} + u_{c_n}^T \sum_{m=1}^{n-1} v_{c_m} \cdot \exp (-\beta(t_n-t_m)) \\ &amp;= \mu_{c_n} + u_{c_n}^T (S_n - v_{c_n}) \end{align*}\]

<p>The problem now is that this scan is not implemented natively in PyTorch, and that is why we are using Jax:</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">u_mark</span> <span class="o">=</span> <span class="n">u</span><span class="p">[</span><span class="n">marks</span><span class="p">]</span>  <span class="c1"># (N, R)
</span><span class="n">v_mark</span> <span class="o">=</span> <span class="n">v</span><span class="p">[</span><span class="n">marks</span><span class="p">]</span>  <span class="c1"># (N, R)
</span><span class="n">mu_mark</span> <span class="o">=</span> <span class="n">mu</span><span class="p">[</span><span class="n">marks</span><span class="p">]</span>  <span class="c1"># (N,)
</span>
<span class="k">def</span> <span class="nf">_scan_op</span><span class="p">(</span><span class="n">e1</span><span class="p">,</span> <span class="n">e2</span><span class="p">):</span>
    <span class="p">(</span><span class="n">u1</span><span class="p">,</span> <span class="n">t1</span><span class="p">),</span> <span class="p">(</span><span class="n">u2</span><span class="p">,</span> <span class="n">t2</span><span class="p">)</span> <span class="o">=</span> <span class="n">e1</span><span class="p">,</span> <span class="n">e2</span> <span class="c1"># each is (R,), scalar
</span>    <span class="n">decay</span> <span class="o">=</span> <span class="n">jnp</span><span class="p">.</span><span class="n">exp</span><span class="p">(</span><span class="o">-</span><span class="n">beta</span> <span class="o">*</span><span class="p">(</span><span class="n">t2</span> <span class="o">-</span> <span class="n">t1</span><span class="p">))</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">u2</span> <span class="o">+</span> <span class="n">decay</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">u1</span><span class="p">,</span> <span class="n">t2</span><span class="p">)</span>
<span class="n">scan_vals</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">lax</span><span class="p">.</span><span class="n">associative_scan</span><span class="p">(</span><span class="n">_scan_op</span><span class="p">,</span> <span class="p">(</span><span class="n">u_mark</span><span class="p">,</span> <span class="n">times</span><span class="p">),</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>

<span class="n">lambdas</span> <span class="o">=</span> <span class="n">mu_mark</span> <span class="o">+</span> <span class="n">jnp</span><span class="p">.</span><span class="nb">sum</span><span class="p">(</span><span class="n">u_mark</span> <span class="o">*</span> <span class="p">(</span><span class="n">scan_vals</span> <span class="o">-</span> <span class="n">v_mark</span><span class="p">),</span> <span class="n">axis</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
<span class="n">A</span> <span class="o">=</span> <span class="n">jnp</span><span class="p">.</span><span class="nb">sum</span><span class="p">(</span><span class="n">jnp</span><span class="p">.</span><span class="n">log</span><span class="p">(</span><span class="n">lambdas</span><span class="p">))</span>
</code></pre></div></div>

<h2 id="complete-code">Complete code</h2>
<p>The full code with a training example is provided at <a href="https://github.com/pablomiralles22/multiprocess-hawkes-jax">pablomiralles22/multiprocess-hawkes-jax</a>.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Implementing the training of a multivariate Hawkes process efficiently with Jax.]]></summary></entry><entry><title type="html">An introduction to contrastive techniques for representation learning</title><link href="https://pablomirallesg.com/blog/contrastive-learning" rel="alternate" type="text/html" title="An introduction to contrastive techniques for representation learning" /><published>2024-07-16T00:00:00+02:00</published><updated>2024-07-16T00:00:00+02:00</updated><id>https://pablomirallesg.com/blog/contrastive-learning</id><content type="html" xml:base="https://pablomirallesg.com/blog/contrastive-learning"><![CDATA[<p>Earlier this year I had to write a technical report on the topic of contrastive
learning
(<a href="/assets/pdf/projects/contrastive-learning-report/report.pdf">link</a>).
This post is an effort to create something more digestible and adequate for
reading. The goal is to give an introduction to representation learning. This
is done in the first section, also discussing its advantages and other related
topics such as self-supervised or transfer learning. The second section is
dedicated to the techniques for representations learning labelled as
“contrastive”. We will go over some of the most important topics: loss
functions, data generation techniques, negative examples and applications.</p>

<h2 id="representations-transfer-learning-and-self-supervised-learning">Representations, transfer learning and self-supervised learning</h2>

<p>Consider some neural network such as the following:</p>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-16-contrastive-learning/nn_repr-480.webp 480w,/assets/img/posts/2024-07-16-contrastive-learning/nn_repr-800.webp 800w,/assets/img/posts/2024-07-16-contrastive-learning/nn_repr-1400.webp 1400w," type="image/webp" sizes="95vw" />
    
    <img src="/assets/img/posts/2024-07-16-contrastive-learning/nn_repr.pdf" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

<p>Neural networks are the composition of transformations, where every
intermediate layer applies a parameterized function to the input coming from
the previous one. The output of each intermediate layer is a new view or
<strong>representation</strong> of the input data. These representations are nothing but
vectors, where the values are features or characteristics of the input. Of
course, neural networks are not interpretable, and these vectors do not
necessarily hold any meaning for us. The network learns to progressively
transform the data into a set of features that is useful to perform the task we
are training it for. For example, if we are training for image classification
of animals, the network might learn a representation with the number of legs,
the shape of the eyes, the color…</p>

<p>At the end of the network, we have a final layer that transforms the last
representation into the prediction, such as a regression value or a probability
distribution over the different classes in our classification problem.</p>

<p>When we train a network on one dataset, we are not only teaching it how to make
predictions, we are teaching it to extract intermediate representations with
features that are useful for the task. This is interesting because these
representations might be useful for other tasks as well. For example, imagine
we want to add a new class of animals in our classification example. Perhaps
the network is already capable of getting features to predict that class. We
can then modify the final head and freeze the rest of the weights of the
network. With fewer parameters to learn, we can train with less data, much
faster and with a smaller energy consumption. In fact, if the learned
representations are rich enough, we might be able to use the same network for a
myriad of different tasks, training only a small head at the end of the
network. This is called <strong>transfer learning</strong>: first <strong>pretraining</strong> for a
<strong>pretext task</strong>, and then <strong>fine-tuning</strong> the model (perhaps modifying the
head) on the <strong>downstream task</strong> we are actually interested in solving. The
following diagram shows this process.</p>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-16-contrastive-learning/transfer-learning.drawio.svg" sizes="95vw" />
    
    <img src="/assets/img/posts/2024-07-16-contrastive-learning/transfer-learning.drawio.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

<h3 id="learning-very-general-representations-self-supervised-learning">Learning very general representations: self-supervised learning</h3>

<p>Another important and related concept is that of <strong>self-supervised learning</strong>.
This term, although poorly coined in my opinion, can be useful to refer to a
particular set of tasks. It refers to supervised tasks where we can generate a
training signal automatically, without the need for human labeling. One example
is the training procedure of the language model BERT<d-cite key="devlin2019BERTPretraining"></d-cite>, called Masked Language Modeling (MLM). In
this task, some parts of the input text are masked, and the model has to
recover them based on the unmasked context. We can then use any available text
on the internet and generate training examples by masking parts of it. This
allows us to generate massive datasets, and thus train very large neural
networks with a nuanced understanding of the distribution of human text. These
large networks learn very general representations, that we can then transfer to
a great number of tasks. In fact, BERT achieved state-of-the-art performance in
several downstream tasks.</p>

<p>Let’s consider another example, this time with images. Images allow us very
simple forms of data augmentation: we can crop, rotate, translate, convert to
greyscale, modify the brightness… We can train a neural network to produce
similar representations for augmented views of the same image, and different
representations for views of different images. Again, we can generate examples
with any image we find, allowing us to create very large datasets and train
massive networks that obtain general representations. As we will see, this is
our first example of contrastive learning.</p>

<h3 id="advantages-and-disadvantages">Advantages and disadvantages</h3>

<p>Representation learning, together with transfer learning and self-supervised
learning, has produced a revolution in the field. Some of the benefits we get
from these techniques are:</p>

<ol>
  <li>
    <p><strong>It can greatly improve performance for some tasks</strong>. Let’s consider the
example of sentiment analysis in text. Textual data is very diverse and
nuanced. Even the same word can represent different sentiments depending on
the context. A small dataset of examples labeled by humans is unlikely to
produce a model with detailed understanding of text. By pretraining a large
model on a difficult task with a gigantic set of examples, we get a model
with nuanced understanding of text, and this is likely to produce better
results on our downstream task as well.</p>
  </li>
  <li>
    <p>Another way to put the previous point is that <strong>it alleviates data
bottlenecks</strong>. If we don’t have enough data to train a decent model for a
task, we can try to pretrain on some pretext task and then fine-tune small
parts of the model with the supervised data we have.</p>
  </li>
  <li>
    <p><strong>It improves generalization</strong>. Let’s consider the sentiment analysis task
again. A pretrained model learns representations for many different tokens
in a lot of different contexts, since the dataset is very large. In this
common feature space, we may find specific features about sentiment, which
may be used by the final head we train on supervised data.</p>

    <p>Since the feature space is common to a large number of tokens and contexts,
even if they do not appear on the supervised data, the model will be able
to extrapolate what they learn.</p>
  </li>
  <li>
    <p><strong>It allows us to use large and powerful models at a fraction of the cost</strong>.
We find many open source models nowadays, and using them for inference
requires much fewer resources than for training.</p>
  </li>
  <li>
    <p><strong>Some model have zero-shot capabilities</strong>. This means that we can perform
inference on tasks it wasn’t trained for without any need for fine-tuning.
We have all seen ChatGPT performing a myriad of different tasks, even though
it was only trained to do next-token prediction. Another example is
CLIP <a class="citation" href="#radford2021LearningTransferable">(Radford et al., 2021)</a>. This model was trained
using contrastive techniques. It encodes text and images into a common
feature vector space. If a text describes an image properly, then their
representation should be close together in the space. This allows us to
perform arbitrary image classification as long as we have textual labels for
the classes. We simply pick the class with the textual label that is closest
to the image embedding.</p>
  </li>
</ol>

<p>Of course, not everything is bright. Training big and general models is very
expensive, and only big companies with large resources can afford to do so. We
may also find that these models are overkill for some tasks. A smaller and more
specialized model can sometimes perform better, specially if the learned
representations aren’t very good for the particular task. Finally, although
zero-shot capabilities are very interesting, performance tends to be subpar.</p>

<h2 id="contrastive-learning">Contrastive learning</h2>

<p>Roughly speaking, in contrastive learning we try to learn vector representation
of the input data by comparing and contrasting examples with each other. For
instances that we deem similar in some sense, we want their vector
representation to be close according to some distance or similarity function.
On the other hand, the vector representation of dissimilar examples should be
further apart. Images provide an easy example. We would expect pictures of
animals to be close together, and far apart from pictures of buildings. Inside
the cluster of pictures of animals, we would also like for pictures of the same
animal to be closer than pictures of different animals, and so on.</p>

<h3 id="basic-concepts-and-notation">Basic concepts and notation</h3>

<p>Let’s establish a framework and notation for the rest of the text.</p>

<ul>
  <li>
    <p>We first consider a set of networks, called <em>encoders</em>, that map the input
data to a fixed sized vector representation of dimension $d$. We may have
one or multiple encoders, depending on the problem. For example, if we are
trying to map both text and images to a common representation space, we
need a different encoder for each domain.</p>

    <p>Mathematically, the $k$-th encoder $e_k$ maps the examples in some input
space $\mathcal{X}_k$ onto a $d$-dimensional vector space:</p>

\[\begin{align*}
  e_k(\cdot;\theta_k): \mathcal{X}_k &amp;\longrightarrow \mathbb{R}^d \\\\
  x &amp;\longmapsto e_k(x; \theta_k )
  .\end{align*}\]

    <p>Here, $\theta_k$ are the parameters of the neural network that we would like
to optimize. In cases where a single encoder is used, we will omit the $k$
subscript.</p>
  </li>
  <li>
    <p>Additionally, we can assume without loss of generality that a final
  head $h_k(\cdot, \eta_k)$ maps these representations to an $m$-dimensional
  metric space, where the notion of distance or similarity is actually
  applied between the vectors. The point of adding this final transformation
  is to separate two tasks: finding a general representation that can be applied
  to other problems and finding a representation in a metric space where the
  notion of distance represents some real-world semantic similarity. The former
  is typically better for transfer learning. If we didn’t need this separation,
  we may assume that this is the identity function.</p>
  </li>
  <li>
    <p>The metric is given by a distance or similarity function $\mathbb{R}^m
  \times \mathbb{R}^m \to \mathbb{R}$, such as the Euclidean distance
  $||z_1 - z_2||$ or the cosine similarity function $\frac{\langle z_1,
  z_2 \rangle}{||z_1|| ||z_2||}$. Similarity functions take larger
  values for similar examples, and will be denoted by $S$, whereas distance
  functions, denoted by $S$, are lower bounded by zero and take smaller
  values for similar instances.</p>
  </li>
  <li>
    <p>A loss function $\mathcal{L}$ is built using this similarity notion
  for a set of encoded examples, penalizing large distances for similar
  instances, and small distances for dissimilar examples. We will take
  a closer look into loss functions in the next section.</p>
  </li>
  <li>
    <p>Finally, given an example $x \in \mathcal{X}$, we will denote
  its vector representation in the general feature space by $v=e(x)$, and its
  embedding in the metric space by $z=h(v)$. Instances that we consider
  similar to an anchor example $x$ will be called positive examples and be
  denoted by a plus superscript $x^+$. We define negative examples
  analogously and denote them by a minus superscript $x^-$. The same
  superscript notation will also be applied to vectors $v$ and $z$.</p>
  </li>
</ul>

<p>This structure is shown graphically for two examples in the following diagram:</p>

<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-16-contrastive-learning/contrastive-learning.drawio.svg" sizes="95vw" />
    
    <img src="/assets/img/posts/2024-07-16-contrastive-learning/contrastive-learning.drawio.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

<h3 id="loss-functions-in-contrastive-learning">Loss functions in contrastive learning</h3>

<p>Loss functions are generated from the distance/similarity metric applied to
different pairs of examples. It should penalize similar examples being far away
and dissimilar examples being close. The latter is as important as the former,
otherwise the network could learn the trivial representation of mapping any
example to the same vector. Let’s take a look at some common functions.</p>

<h4 id="pair-loss">Pair loss</h4>

<p>Let $x \in \mathcal{X}$ be an example and $z$ its embedding in the metric
space. The <em>pair loss</em> <a class="citation" href="#chopra2005LearningSimilarity">(Chopra et al., 2005)</a> is defined
for pairs of examples, differing for positive and negative ones:</p>

\[\begin{cases}
    \mathcal{L}(x, x^+) &amp;= D(z, z^+)^2 \\
    \mathcal{L}(x, x^-) &amp;= \max (0, \varepsilon - D(z, z^-)^2),
  \end{cases}\]

<p>where $D$ is some distance function such as the Euclidean distance. By
minimizing this expression, the distance to positive samples should be close to
zero, and dissimilar instances should be separated by a margin of at least
$\varepsilon$. The function is plotted in the following figure.</p>

<!-- <figcaption>
    Plot of the pair loss function for positive (left) and negative (right)
    examples. Arrows represent gradient direction and norm.
</figcaption> -->

<figure class="class1 class2">
<div style="display: flex; justify-content: center; align-items: flex-start; gap: 20px; margin-top: 20px;">
    <div style="flex: 1; text-align: center;">
        



<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_pos-480.webp 480w,/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_pos-800.webp 800w,/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_pos-1400.webp 1400w," type="image/webp" sizes="95vw" />
    
    <img src="/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_pos.pdf" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

    </div>
    <div style="flex: 1; text-align: center;">
        



<figure>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    
      <source class="responsive-img-srcset" srcset="/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_neg-480.webp 480w,/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_neg-800.webp 800w,/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_neg-1400.webp 1400w," type="image/webp" sizes="95vw" />
    
    <img src="/assets/img/posts/2024-07-16-contrastive-learning/pair_loss_neg.pdf" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();" />
  </picture>

  
</figure>

    </div>
</div>
  <figcaption>Plot of the pair loss function for positive (left) and negative (right) examples. Arrows represent gradient direction and norm.</figcaption>
</figure>

<h4 id="triplet-loss">Triplet loss</h4>

<p>The <em>triplet loss</em> <a class="citation" href="#schroff2015FaceNetunified">(Schroff et al., 2015)</a> tries instead to
enforce that the distance between the anchor and positive examples is smaller
than the distance between the anchor and negative examples, with a difference
margin of at least $\varepsilon$:</p>

\[\label{eq:triplet-loss}
\mathcal{L}(x, x^+, x^-) = \max(0, D(z,z^+)^2 - D(z,z^-)^2 + \varepsilon)
.\]

<p>In this last loss function, a full training example requires now both a similar
and a dissimilar instance. However, the number of interactions between
different examples is still very limited. The use of many negative examples,
specially those that are hard for the model, are crucial for a good and
efficient learning process. This makes sense intuitively: there are many more
ways in which images can be different than similar.</p>

<h4 id="lifted-embedding-loss">Lifted Embedding Loss</h4>

<p>Increasing the number of interactions between instances at once, Song et al.
proposed the <em>Lifted Embedding loss</em> <a class="citation" href="#song2016DeepMetric">(Song et al., 2016)</a>. If
$x_1, \cdots, x_n$ is our set of examples, and $P$ and $N$ are the sets of pairs
of examples that are considered similar and dissimilar, respectively, then the
loss function is given as</p>

\[\label{eq:lifted-structure-loss}
  \mathcal{L} (N, P) = \frac{1}{2 |P|} \sum_{(i,j) \in P} \max (0, L_{i,j})^2
,\]

<p>where</p>

\[L_{i,j} = D_{i,j} + \log \left( 
    \sum_{(i,k) \in N} e^{\varepsilon - D_{i,k}} +
    \sum_{(j,l) \in N} e^{\varepsilon - D_{j,l}}
   \right)
,\]

<p>and $D_{i,j} = D(z_i, z_j)$. The expression for $L_{i,j}$ is
actually used because it is a smooth upper bound for</p>

\[\hat{L}_{i,j} = D_{i,j} + \max \left( 
    \max_{(i,k) \in N} \varepsilon - D_{i,k},
    \max_{(j,l) \in N} \varepsilon - D_{j,l}
   \right)
,\]

<p>so this loss can be interpreted as an adaptation to the triplet loss, trying to
mine the hardest negative example of the set for each positive pair, and also
squaring the result. As usual in deep learning, instead of using the full set
of examples, the loss is approximated with a batch of smaller size.</p>

<h4 id="binary-noise-contrastive-estimation-nce-loss">Binary Noise-Contrastive Estimation (NCE) loss</h4>

<p>Let’s now take a probabilistic perspective. Let $X_1$ and $X_2$ be two
random variables that take values in our domain of examples $\mathcal{X}$
(e.g. images). Let $Y$ be another random variable that takes the value of
$1$ if $X_1$ and $X_2$ are similar according to our concept of similarity,
and $0$ otherwise. It follows a Bernoulli distribution, and we can try to
approximate its conditional probability mass function with our network as</p>

\[\hat{p} (1 | x_1, x_2) = \sigma ( S(z_1, z_2) )
,\]

<p>where $\sigma$ is the sigmoid function. If $q^+(\cdot, \cdot)$ and $q^-(\cdot,
\cdot)$ are the joint probability density functions of similar and dissimilar
instances, respectively, then the Binary Noise-Contrastive Estimation
(NCE) <a class="citation" href="#gutmann2010Noisecontrastiveestimation">(Gutmann &amp; Hyvärinen, 2010)</a> loss is given by
minimizing the expected negative log-likelihood:</p>

\[\begin{gathered}
  \label{eq:bin-nce}
  \mathcal{L}_{Bin-NCE} =
  -\mathbb{E}_{q^+} \log \hat{p} (1| x_1, x_2) \\
  -\mathbb{E}_{q^-} \log (1-\hat{p} (1| x_1, x_2))
,
\end{gathered}\]

<p>where the expected value would be approximated by its population mean (e.g.
with a single batch). Keeping the previous notation for the sets of positive
pairs $P$ and negative pairs $N$, this yields:</p>

\[\begin{gathered}
  \label{eq:bin-nce-2}
  \mathcal{L}_{Bin-NCE} =
  -\frac{1}{|P|} \sum_{(i,j) \in P} \log \sigma(S(z_i, z_j))  \\
  -\frac{1}{|N|} \sum_{(i,j) \in N} \log (1-\sigma(S(z_i, z_j)))
.
\end{gathered}\]

<h4 id="infonce">InfoNCE</h4>

<p>A more recent loss function is InfoNCE <d-cite key="oord2019RepresentationLearning"></d-cite>. In their setting, instead of
considering a binary classification problem they assume a ranking one. Having
fixed an anchor instance $x$, let $x_0^+,x_1^-, \cdots,x_n^-$ be a set of
possible similar instances, where only $x_0^+$ is a positive example. The
problem becomes one of ranking which example is more likely to be positive. The
probability of each of the samples can be approximated by a softmax operation
on a similarity score with respect to $x$:</p>

\[\label{eq:info-nce-p}
\hat{p} (i|x) = \frac{\exp(S(x, x_i))}{\sum_{j = 0}^{n} \exp(S(x, x_j))}
.\]

<p>Minimizing the negative log-likelihood of the true positive yields:</p>

\[\label{eq:info-nce}
\mathcal{L}_{InfoNCE} = 
  - \mathbb{E} \log \frac{\exp(S(x, x_0^+))}{\sum_{j = 0}^{n} \exp(S(x, x_j))}
.\]

<p>As we will see, it is a common setting to have batches of pairs of similar
instances $(x_1,x_1^\prime),\cdots,(x_n,x_n^\prime)$, where instances across
pairs are considered to be dissimilar. We can compute a similarity matrix
$\mathcal{S}  = (S(z_i, z_j^\prime))_{i,j}$, where the main diagonal values
should be high and the rest should be low. In this case, one can calculate the
InfoNCE across rows or columns. It is also possible to average both options,
yielding a <em>symmetric InfoNCE</em> loss.</p>

<h4 id="nt-xent">NT-Xent</h4>

<p>A temperature parameter $\tau$ can be included in the sotfmax operation,
transforming the softmax probability distribution into</p>

\[P(i|x) = \frac{\exp(S(x, x_i) / \tau)}{\sum_{j = 0}^{n} \exp(S(x, x_j) / \tau)}
.\]

<p>A small value of $\tau$ makes the softmax sharper, and small differences
between the similarity of positive and negative examples already produce a
high likelihood. A large value of $\tau$ forces the difference in similarity to
be large. This parameter can be viewed as the margin parameter in previous
functions. This modified InfoNCE loss is called <em>NT-Xent</em> (normalized
temperature-scaled cross entropy loss) <a class="citation" href="#chen2020SimpleFramework">(Chen et al., 2020)</a>.</p>

<h3 id="generating-data-for-contrastive-learning">Generating data for contrastive learning</h3>

<p>We have already seen how to create a loss function to train our model, but how
do we get appropriate data? In most cases, the question is how to generate
pairs of <em>similar</em> examples, as you can often get away with the assumption that
examples in different pairs are dissimilar. This is the case when you have a
huge domain, such as a large amount of internet images. Getting two similar
examples in the same batch by sampling randomly is unlikely. Still, with this
assumption we could incur in false negatives, something we will discuss in the
next section. Let’s see now some of the ways in which datasets are generated
for contrastive learning.
<!-- This setting is called *instance-level discrimination*<d-cite -->
<!-- key="wu2018UnsupervisedFeature"/>, as we assume that each instance forms its -->
<!-- own class. We will now list some of the ways in which we can generate datasets -->
<!-- for contrastive learning. --></p>

<h4 id="human-supervision">Human supervision</h4>

<p>Of course, human annotation is always an option, albeit a very expensive one.
Unfortunately, this is sometimes necessary. We previously talked about CLIP, a
model that learned representations for text and image, mapping a text that
correctly describe an image to an embedding that is close to the embedding of
the image. In this case, there is no way around getting someone to create
captions for different images (although in the future synthetic data generation
with multimodal models might be an option).</p>

<p>One way to reduce the amount of data necessary when human labeling is required
is to first pretrain the encoders with some other self-supervised technique.
For example, in CLIP, you could train the text and image encoders separately
with a denoising task, and then use the supervised data to align the
representations of both encoders to a common metric space.</p>

<h4 id="self-supervision">Self-supervision</h4>

<p>What if we want to generate large amounts of data from existing sources
automatically? As explained earlier, we will generate pairs of similar
instances, and assume that images in different pairs are dissimilar. The
following are just some examples:</p>

<ul>
  <li>
    <p><strong>Data augmentation</strong>. In the particular context of contrastive learning, a
  transformation that does not change the instance semantically can be applied to
  generate positive examples. To illustrate this, assume that we are
  contrasting images, and we want to map images based on the concepts inside
  them. Then if we crop, blur, or saturate the color of an image of a cat, it
  is still an image of a cat. This way, augmentations of similar or equal
  instances are also similar, whereas augmentations of dissimilar examples
  are also dissimilar.</p>

    <p>In the case of images, we find transformations such as rotations,
  translations, cutouts, cropping and resizing, blurring, applying noise…
  These were used, for example, in <a class="citation" href="#chen2020SimpleFramework">(Chen et al., 2020)</a>.</p>

    <p>Textual data is a bit more complex. Fang et al.<d-cite key="fang2020CERTContrastive"></d-cite> use automatic back-translation, that is,
  translation to a different language and then back to the source language,
  to generate pairs of different but semantically equivalent segments of
  text. Many other techniques can be used: changing characters, masking
  words, adding noise, changing words for synonyms…</p>

    <p>Careful exploration of data augmentation techniques can be very important.
  For example, Chen et al. <a class="citation" href="#chen2020SimpleFramework">(Chen et al., 2020)</a> found that
  the training of SimCLR was very sensitive to the choice of data
  augmentation techniques.</p>
  </li>
  <li>
    <p><strong>Multi-sensor</strong>. When multiple inputs are captured simultaneously, we can
  use all inputs from corresponding times. For example, capturing the same
  image from different angles with multiple cameras, or the audio and images
  in videos.</p>
  </li>
  <li>
    <p><strong>Continuity</strong>. This can be applied to domains where there
  are sequences with some continuity. For example, videos are sequences of
  images where most contiguous frames are almost equal. We can take images
  very close in time and assume they are very likely to be similar.</p>
  </li>
</ul>

<h3 id="discussion-on-negative-examples">Discussion on negative examples</h3>

<p>The loss functions we have seen thus far use both positive and negatives
examples. We already gave a theoretical reason for this: if negative examples
were not used, a trivial representation mapping everything to one vector would
obtain perfect performance. There is empirical evidence showing that
performance is increased from contrasting with many negative examples. For
example, Chen et al. <a class="citation" href="#chen2020SimpleFramework">(Chen et al., 2020)</a> found that training
with very large batch sizes greatly improved performance. In their setting, all
other examples in the batch were considered as negative, and thus the number of
negatives per example scaled with the batch size.</p>

<p>The concept of negative examples or, more generally, preventing
representational collapse, is quite important in contrastive learning, and
there are some topics around it that are worth discussing.</p>

<h4 id="false-negatives-in-self-supervised-contrastive-learning">False negatives in self-supervised contrastive learning</h4>

<p>In the data generation section we already talked at the possibility of false
negatives when there is no supervision. This is because we are drawing from the
full distribution of examples instead of the distribution of negative examples,
creating a bias. Some work has been done on alleviating this while not requiring
manual labels for negatives. While we won’t get into detail, the reader may
find an example in the <em>Debiased Contrastive loss</em> proposed in <d-cite key="chuang2020DebiasedContrastive"></d-cite>.</p>

<h4 id="alleviating-hardware-bottlenecks-for-large-amounts-of-negative-samples">Alleviating hardware bottlenecks for large amounts of negative samples</h4>

<p>The use of examples in the same batch as negatives presents
a major drawback in terms of computing resources. If we want to use many
negative examples, we are forced to select a very large batch size, requiring a
lot of GPU memory. Some work has been done on decoupling batch size and the
number of negatives by sampling negatives from an <em>offline memory bank</em>. In
this setting, an encoded representation is kept on-disk for some or all
examples, and the loss function is not back-propagated through them. As the
encoder is updated in the training process, the representations on-disk get
outdated. The main difference between approaches lies in the method to keep
these offline representations updated as the encoder is optimized.</p>

<p>Wu et al. <a class="citation" href="#wu2018UnsupervisedFeature">(Wu et al., 2018)</a> sampled negative
representations randomly from a memory bank with the full dataset. At the end
of each epoch, all the representations in the memory bank are updated with the
new checkpoint of the model.</p>

<p>He et al. <a class="citation" href="#he2020MomentumContrast">(He et al., 2020)</a> proposed keeping a queue with
a fixed number of mini-batches. After processing a mini-batch, the new examples
are added to the queue, and the oldest mini-batch is removed. The queue is used
to sample negative examples for the current mini-batch. Since this alone
resulted in poor empirical performance, they separated the online encoder that
is being trained from an offline encoder that produces the representations for
the queue. The parameters of the offline encoder are updated through a momentum
update rule with the parameters of the online one:</p>

\[\label{eq:moco} 
  \theta_{off} \leftarrow 
    \alpha \theta_{off} + (1-\alpha) \theta_{on},
  \qquad \alpha \in [0,1)
.\]

<p>This smoother update yielded better empirical performance.</p>

<h4 id="hard-negative-mining">Hard negative mining</h4>

<p>While increasing the number of negative examples has been observed to
improve performance, this might be due to the increased probability of
finding meaningful negatives to learn from.</p>

<p>Conceptually, we might intuit that it is more difficult for a model to
distinguish between a dog and a cat than between a dog and a building. In
practice, we could select hard negative examples based on their representation.
Examples with close representations are difficult to distinguish for the model.
Kalantidis et al. <a class="citation" href="#kalantidis2020HardNegative">(Kalantidis et al., 2020)</a> make use of this,
together with some data mixing techniques.</p>

<p>However, hard negative mining has some disadvantages, such as the
increased time complexity from sampling close neighbours and the
increased probability of drawing false negatives in the self-supervised
setting as the encoder gets better.</p>

<h4 id="are-negative-examples-really-necessary">Are negative examples really necessary?</h4>

<p>If the only reason to use negative examples is to prevent the
representations from collapsing onto one single vector, then other
techniques to prevent it could allow us to remove negative examples.</p>

<p>The work by Grill et al. <a class="citation" href="#grill2020BootstrapYour">(Grill et al., 2020)</a> goes in this
line. They used two neural networks, an online (predictive) network and a
target network, similar to Deep Q Learning. They use only positive examples,
and for each pair the online network tries to predict the metric representation
from the target network. The parameters of the target network are updated after
every iteration with an exponential moving average of the online parameters:</p>

\[\theta_{target}  \gets \alpha \theta_{target}  + (1 - \alpha) \theta_{online} 
.\]

<p>It is not obvious to me that this approach would not converge to a collapsed
representation. The authors argue that the update to the target parameters is
not exactly according to the gradient of the loss with respect to
$\theta_{target}$. They did get good performance in downstream image
classification tasks. There is some discussion on informal mediums (see 
<a href="https://imbue.com/research/2020-08-24-understanding-self-supervised-contrastive-learning/">this blog post</a>
and preprint <a class="citation" href="#richemond2020BYOLworks">(Richemond et al., 2020)</a>) on whether batch
normalization is the cause of preventing representational collapse, but I
haven’t found peer-reviewed work on the topic.</p>

<p>SimSiam <a class="citation" href="#chen2021ExploringSimple">(Chen &amp; He, 2021)</a> is an even simpler technique
that successfully avoided representational collapse. I deviate from their
notation in the explanation to keep the one in this post. If $\hat{e}=e \circ
h$ is the complete encoder onto the metric space, and $\hat{h}$ is an additional
head they call the predictor, then their loss functions for similar instances
$x_1$ and $x_2$ is given by the formula:</p>

\[z_1 = \hat{e} (x_1),\quad z_2 = \hat{e} (x_2)\]

\[p_1 = \hat{h} (z_1),\quad p_2 = \hat{h} (z_2)\]

\[\mathcal{L} = \frac{D(p_1, SG(z_2))}{2} + \frac{D(p_2, SG(z_1))}{2}
,\]

<p>where $SG$ is the stop-gradient function, preventing backpropagation through
that branch of computation, and $D$ is the negative cosine similarity. Again,
how this asymmetry through the additional head and stopping gradient
propagation achieves its objective of avoiding representation collapse is not
fully understood (to the best of my knowledge).</p>

<p>Bardes et al. <a class="citation" href="#bardes2022vicreg">(Bardes et al., 2022)</a> proposed a much more intuitive
approach, using VICReg (Variance-Invariance-Covariance regularization). Let
$(z_1,z_1^\prime),\cdots,(z_n,z_n^\prime)$ be $d$-dimensional metric
representations of positive pairs in a single batch. Let $C(Z)$ be the
covariance matrix <strong>of the features</strong> for the matrix $Z=(z_1,\cdots,z_n) \in
\mathbb{R}^{n \times d}$, that is,</p>

\[C(Z) = \frac{1}{n-1} \sum_{i=1}^{n} (z_i - \overline{z})(z_i - \overline{z})^T
,\]

<p>where $\overline{z} = \frac{1}{n} \sum_{i=1}^{n} z_i$ is the empirical mean. Then the
loss function has the following three terms:</p>

<ul>
  <li>
    <p><strong>Invariance</strong>. Forces the positive examples to be close together:</p>

\[\mathcal{I} = \frac{1}{n}\sum_{i=1}^{n} \| z_i - z_i^\prime \|^2
  .\]
  </li>
  <li>
    <p><strong>Variance</strong>. Forces the variance of each feature to be positive, above some
  threshold value:</p>

\[\begin{gathered}
  \mathcal{V} =
  \frac{1}{d} \sum_{j=1}^{d}  \max \left(0, \sqrt{C(Z)_{j,j} + \epsilon} \right) + \\
  \frac{1}{d} \sum_{j=1}^{d}  \max \left(0, \sqrt{C(Z^\prime)_{j,j} + \epsilon} \right)
  .\end{gathered}\]
  </li>
  <li>
    <p><strong>Covariance</strong>. Forces the features to be uncorrelated:</p>

\[\mathcal{C} =
  \frac{1}{d} \sum_{i,j=1; i\neq j}^{d}  C(Z)_{i,j}^2 +
  \frac{1}{d} \sum_{i,j=1; i\neq j}^{d}  C(Z^\prime)_{i,j}^2
  .\]
  </li>
</ul>

<p>The final loss is a weighted sum of the previous terms:</p>

\[\mathcal{L} = \mathcal{I}  + \lambda \mathcal{V}  + \mu \mathcal{C} 
,\]

<p>where $\lambda$ and $\mu$ are positive hyperparameters. The workings of this
approach are much more intuitive and clear: the regularization terms explicitly
prevent the representation from collapsing, as collapsed representations have
zero feature variance. Decorrelating the features might be useful for
interpretability purposes.</p>

<h3 id="applications">Applications</h3>

<p>We have already talked about the benefits of representation learning in general.
But does contrastive learning have any particular advantages? This is what we
discuss here.</p>

<p>The first advantage is that it provides other training ideas for representation
learning. These techniques have shown great success, reaching state-of-the-art
performance in semi-supervised and transfer learning. The SimCLR model for
images <a class="citation" href="#chen2020SimpleFramework">(Chen et al., 2020)</a> is an example of this.</p>

<p>A second advantage is the metric representation that they learn. These
representations can later be used in very efficient<d-footnote>There are
techniques to approximately find the closest vectors to the input in a vector
database, such as Locality-Sensitive Hashing or k-d Trees.</d-footnote> vector
searches, allowing us to search for examples in our database that are
<strong>semantically</strong> similar to the input. Thinking of search engines for examples,
we can now find texts that talk about something similar to our search prompt,
without the need to use any common word. With multilingual models, we might
even search semantically in different languages at the same time. We find many
open source language models that have been trained for this semantic encoding,
and that we can readily apply to any search application.</p>

<p>Although not exclusive to contrastive learning, another advantage is that these
techniques allow us to align different encoders in a common representation
space, in particular encoders of different modalities. This is the case of the
CLIP <a class="citation" href="#radford2021LearningTransferable">(Radford et al., 2021)</a> model we have already
talked about.</p>

<p>Finally, models trained through contrastive learning have zero-shot
classification capabilities. If you have some anchor examples that represent a
category, you can classify a new example by selecting the category with the
closest metric representation. In the case of CLIP, since it has a text
encoder, you can provide any textual label that describes the category, and
apply this inference technique. If there are no textual encoders, you could
still use a small set of labeled data to create a metric representation of
each class. Of course, performance is not as good as with fully-trained models,
specially for very niche categories, but it might still be useful with very few
resources.</p>

<h2 id="conclusions">Conclusions</h2>

<p>That it is for this post. I hope you found it useful and not too boring! As
always, the best way to learn about anything is to get your hands dirty. In
this case, you can try programming some training loop with a small dataset and
a loss function, such as NT-Xent. You can also try programming some of the
techniques to avoid representational collapse and see how they perform. Don’t
doubt in hitting me up with any feedback or corrections!</p>

<h2 id="references">References</h2>

<ol class="bibliography"><li><span id="bardes2022vicreg">Bardes, A., Ponce, J., &amp; LeCun, Y. (2022). VICReg: Variance-Invariance-Covariance Regularization for Self-Supervised Learning. <i>International Conference on Learning Representations</i>.</span></li>
<li><span id="chen2020SimpleFramework">Chen, T., Kornblith, S., Norouzi, M., &amp; Hinton, G. (2020). A Simple Framework for Contrastive Learning of Visual Representations. <i>Proceedings of the 37th International Conference on Machine Learning</i>, 1597–1607.</span></li>
<li><span id="chen2021ExploringSimple">Chen, X., &amp; He, K. (2021). Exploring Simple Siamese Representation Learning. <i>2021 IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)</i>, 15745–15753. https://doi.org/10.1109/CVPR46437.2021.01549</span></li>
<li><span id="chopra2005LearningSimilarity">Chopra, S., Hadsell, R., &amp; LeCun, Y. (2005). Learning a Similarity Metric Discriminatively, with Application to Face Verification. <i>2005 IEEE Computer Society Conference on Computer Vision and Pattern Recognition (CVPR’05)</i>, <i>1</i>, 539–546. https://doi.org/10.1109/CVPR.2005.202</span></li>
<li><span id="chuang2020DebiasedContrastive">Chuang, C.-Y., Robinson, J., Lin, Y.-C., Torralba, A., &amp; Jegelka, S. (2020). Debiased Contrastive Learning. <i>Advances in Neural Information Processing Systems</i>, <i>33</i>, 8765–8775.</span></li>
<li><span id="devlin2019BERTPretraining">Devlin, J., Chang, M.-W., Lee, K., &amp; Toutanova, K. (2019). BERT: Pre-Training of Deep Bidirectional Transformers for Language Understanding. <i>Proceedings of the 2019 Conference of the North American Chapter of the Association for Computational Linguistics: Human Language Technologies, Volume 1 (Long and Short Papers)</i>, 4171–4186. https://doi.org/10.18653/v1/N19-1423</span></li>
<li><span id="fang2020CERTContrastive">Fang, H., Wang, S., Zhou, M., Ding, J., &amp; Xie, P. (2020). <i>CERT: Contrastive Self-Supervised Learning for Language Understanding</i> (Number arXiv:2005.12766). arXiv. https://doi.org/10.48550/arXiv.2005.12766</span></li>
<li><span id="grill2020BootstrapYour">Grill, J.-B., Strub, F., Altché, F., Tallec, C., Richemond, P., Buchatskaya, E., Doersch, C., Avila Pires, B., Guo, Z., Gheshlaghi Azar, M., Piot, B., kavukcuoglu, koray, Munos, R., &amp; Valko, M. (2020). Bootstrap Your Own Latent - A New Approach to Self-Supervised Learning. <i>Advances in Neural Information Processing Systems</i>, <i>33</i>, 21271–21284.</span></li>
<li><span id="gutmann2010Noisecontrastiveestimation">Gutmann, M., &amp; Hyvärinen, A. (2010). Noise-Contrastive Estimation: A New Estimation Principle for Unnormalized Statistical Models. <i>Proceedings of the Thirteenth International Conference on Artificial Intelligence and Statistics</i>, 297–304.</span></li>
<li><span id="he2020MomentumContrast">He, K., Fan, H., Wu, Y., Xie, S., &amp; Girshick, R. (2020). Momentum Contrast for Unsupervised Visual Representation Learning. <i>2020 IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR)</i>, 9726–9735. https://doi.org/10.1109/CVPR42600.2020.00975</span></li>
<li><span id="kalantidis2020HardNegative">Kalantidis, Y., Sariyildiz, M. B., Pion, N., Weinzaepfel, P., &amp; Larlus, D. (2020). Hard Negative Mixing for Contrastive Learning. <i>Advances in Neural Information Processing Systems</i>, <i>33</i>, 21798–21809.</span></li>
<li><span id="oord2019RepresentationLearning">van den Oord, A., Li, Y., &amp; Vinyals, O. (2019). <i>Representation Learning with Contrastive Predictive Coding</i> (Number arXiv:1807.03748). arXiv.</span></li>
<li><span id="radford2021LearningTransferable">Radford, A., Kim, J. W., Hallacy, C., Ramesh, A., Goh, G., Agarwal, S., Sastry, G., Askell, A., Mishkin, P., Clark, J., Krueger, G., &amp; Sutskever, I. (2021). Learning Transferable Visual Models From Natural Language Supervision. <i>Proceedings of the 38th International Conference on Machine Learning</i>, 8748–8763.</span></li>
<li><span id="reimers2019SentenceBERTSentencea">Reimers, N., &amp; Gurevych, I. (2019). Sentence-BERT: Sentence Embeddings Using Siamese BERT-Networks. <i>Proceedings of the 2019 Conference on Empirical Methods in Natural Language Processing and the 9th International Joint Conference on Natural Language Processing (EMNLP-IJCNLP)</i>, 3982–3992. https://doi.org/10.18653/v1/D19-1410</span></li>
<li><span id="richemond2020BYOLworks">Richemond, P. H., Grill, J.-B., Altché, F., Tallec, C., Strub, F., Brock, A., Smith, S., De, S., Pascanu, R., Piot, B., &amp; Valko, M. (2020). <i>BYOL Works Even without Batch Statistics</i> (Number arXiv:2010.10241). arXiv.</span></li>
<li><span id="schroff2015FaceNetunified">Schroff, F., Kalenichenko, D., &amp; Philbin, J. (2015). FaceNet: A Unified Embedding for Face Recognition and Clustering. <i>2015 IEEE Conference on Computer Vision and Pattern Recognition (CVPR)</i>, 815–823. https://doi.org/10.1109/CVPR.2015.7298682</span></li>
<li><span id="song2016DeepMetric">Song, H. O., Xiang, Y., Jegelka, S., &amp; Savarese, S. (2016). Deep Metric Learning via Lifted Structured Feature Embedding. <i>2016 IEEE Conference on Computer Vision and Pattern Recognition (CVPR)</i>, 4004–4012. https://doi.org/10.1109/CVPR.2016.434</span></li>
<li><span id="wu2018UnsupervisedFeature">Wu, Z., Xiong, Y., Yu, S. X., &amp; Lin, D. (2018). Unsupervised Feature Learning via Non-Parametric Instance Discrimination. <i>2018 IEEE/CVF Conference on Computer Vision and Pattern Recognition</i>, 3733–3742. https://doi.org/10.1109/CVPR.2018.00393</span></li>
<li><span id="zbontar2021BarlowTwins">Zbontar, J., Jing, L., Misra, I., LeCun, Y., &amp; Deny, S. (2021). Barlow Twins: Self-Supervised Learning via Redundancy Reduction. <i>Proceedings of the 38th International Conference on Machine Learning</i>, 12310–12320.</span></li>
<li><span id="zhang2022how">Zhang, C., Zhang, K., Zhang, C., Pham, T. X., Yoo, C. D., &amp; Kweon, I. S. (2022). How Does SimSiam Avoid Collapse without Negative Samples? A Unified Understanding with Self-Supervised Contrastive Learning. <i>International Conference on Learning Representations</i>.</span></li></ol>]]></content><author><name></name></author><category term="deep-learning" /><category term="contrastive-learning" /><summary type="html"><![CDATA[A look at some of the basic research on contrastive learning]]></summary></entry><entry><title type="html">How to do well in your Mathematics and Computer Science courses</title><link href="https://pablomirallesg.com/blog/how-to-do-well-in-math-and-cs" rel="alternate" type="text/html" title="How to do well in your Mathematics and Computer Science courses" /><published>2024-07-07T00:00:00+02:00</published><updated>2024-07-07T00:00:00+02:00</updated><id>https://pablomirallesg.com/blog/how-to-do-well-in-math-and-cs</id><content type="html" xml:base="https://pablomirallesg.com/blog/how-to-do-well-in-math-and-cs"><![CDATA[<p>During my second year at university I wrote a post about my studying process. A
lot has changed since then, and I wanted to update it and make it a bit more
mature. In this post I try to give an idea of what worked for me, both in terms
of learning and of getting good grades. Unfortunately, these two things are at
times uncorrelated.</p>

<p>I acknowledge that some of the things I advice here may take a lot of time, and
time can be scarce, specially in a joint bachelor program where you take
1.2-1.4x the workload. You may take what works best for you given the time you
have. It is also important to be focused during the time you put in. In this
regard, I have two pieces of advice. The first one is to be consistent with
your schedule, as building the stamina to study for hours without distractions
takes time. The second is minimizing the time in very stimulant activities such
as social media.</p>

<p>The post has three sections. In the first one, I talk about the main general
principles that guided my studying process and projects, helping me prioritize
what’s important. The second one describes a general overview of a full
semester for me. Finally, I discuss some other topics that typically come out
when talking about studying, but that do not make, in my opinion, a big
difference.</p>

<h2 id="general-principles-and-techniques">General principles and techniques</h2>

<h3 id="solving-problems">Solving problems</h3>

<p>The post could probably end right here. The single most importance thing you
can do for yourself is solving problems, specially in mathematics, where you
need to grasp complex concepts and apply them to solve non-algorithmic
exercises. I have seen a lot of colleagues spending large amounts of time
trying to get a deep understanding on the theory before getting to the problem
sheets, leaving very little time for the latter.</p>

<p>I prefer doing the opposite: I first get a very broad overview of the topic,
and then start solving problems. This will force me to go back to the theory,
search for what I need, and organize and connect ideas together. When the exam
is close, I go back and study the theory thoroughly, but by this time I already
knew most of it.</p>

<p>This way, you are actively engaging with the information, because you need it
for a specific goal. You need to understand, filter, connect and organize the
information in order to solve your problem. This is much more entertaining than
passively going through the text without any purpose or emotional attachment,
and I find that information sticks in my mind much better when practicing these
higher order thinking skills (organizing and connecting concepts to form
arguments vs. understanding other’s arguments).</p>

<p>Another reason to prioritize problems is that this is the skill you will
actually get paid for in the future. If you are a master of consuming and
understanding information but not of reusing it for a particular problem or
situation, you will provide little value for others.</p>

<p>One practical consideration is deciding how much time you spend on any given
problem if you are not able to solve it. This again will depend on how much
time you have. I prefer spending at least 10-30 minutes on each problem, and
trying again for several days. I find that the more effort I put into a
problem, the better that particular pattern is encoded in my brain. Of course,
if you are pressed for time, you will want to look at solutions much sooner. If
solutions aren’t provided in your class, you can always share solutions with
your classmates.</p>

<p>Sometimes you want to learn something relatively complex but your professor
doesn’t provide exercises or other ways to practice. In these cases you can
find your own ways. You may find exercises in books, try to implement
algorithms, create detailed concept mindmaps…</p>

<p>Finally, sometimes you have to complete algorithmic exercises, where you always
have to do the same steps, and the difficulty lies in not making stupid
mistakes. I try not to lose a lot of time on these, as you don’t get a lot of
learning from them. I often do two or three of these exercises each day before
the exam, so I am used to them and minimize the chances of dumb errors
reducing the grade.</p>

<h3 id="flashcards-for-rote-memorization">Flashcards for rote memorization</h3>

<p>While mathematics and computer science are two fields where you can mostly get
away with understanding everything, you may still need to memorize things from
time to time. This is the case of proofs that you may get asked during the
exam, or questions about an IEEE standard if your professor wants to make
everyone’s life miserable.</p>

<p>In this case, flashcard apps like Anki are the way to go. They force you to
actively recall the information and automatically space out the flashcards so
you don’t over study (search active recall and spaced repetition if you are not
familiar with these concepts). Mathematical proofs are a bit tricky because
they can be quite large. You can divide them in parts if you prefer to, and
write them out when you get the flashcards.</p>

<h3 id="spacing">Spacing</h3>

<p>I try to space both my flashcard reviews and my practice sessions with
exercises. While I recommend a decent block of time (at least 1-2 hours) for a
practice session on a subject, I also like to interleave subjects and touch
each subject in different days, allowing the information to rest in my mind
during the night. As an example, if I have to do exercises for two classes and
I have two days, I prefer doing one half of the exercises for each class both
days. This also allows me to try unsolved exercises again.</p>

<h3 id="going-the-extra-mile-in-projects">Going the extra mile in projects</h3>

<p>In computer science you typically have to complete practical assignments. While
sometimes useless, they often serve as a way to learn the topics for the class.
I try, specially when I like the topic, to do something extra, something that I
find motivating. This will make you learn more, have better projects for an
initial portfolio, and help you lock in the highest grade. Some professors
grade the assignments comparatively, so if other students do something extra
and you don’t, you may get penalized.</p>

<h2 id="an-overview-of-a-semester">An overview of a semester</h2>

<p>After seeing the general principles you may want to know how they look like
during a full semester. In Spain most people do 5 courses simultaneously
spread over a 5 month period. How you structure your studying will obviously
depend on the structure of your program.</p>

<h3 id="during-the-semester">During the semester</h3>

<p>The semester could be a bit chaotic. The only plan was to keep up with the
exercises, specially for math courses, while also completing the assignments
for computer science. Ideally I would also create theory flashcards along the
way, but I almost never managed to.</p>

<p>In the middle of the semester we would usually have midterms. I approached them
exactly the same way as finals, with the process described in the next section.
Since we still had classes I would apply the process during the previous
weekend, and whenever I could during weekdays, although they were busy with
classes. During these periods you will have to prioritize a lot, and catch up
with the work you leave behind later on, often during holidays.</p>

<h3 id="during-final-exams">During final exams</h3>

<p>Finals usually take one full month, with 2-6 days between each exam. When
finals start, it would be ideal to have finished almost all practice problems
already, except perhaps algorithmic ones. This often requires spending the
previous holiday (Christmas and Easter) completing them.</p>

<p>In the days between exams I often only study for the next exam, except if some
other exam is particularly difficult or the exam calendar is very imbalanced. I
dedicate each day to perform one full review of the theory, one pass over each
type of algorithmic exercise and some exams from previous years, if available.
If I didn’t manage to complete all problems before this period, I’ll start with
that while also studying the theory.</p>

<p>The first day I almost always had to create flashcards from scratch, which was
a bit painful. I swear revising becomes easier afterwards. Although I often
ended up not spacing but cramming my flashcards, I already have a good idea of
the theory from other activities. If we had a high theory load I would split
revisions across days, but I always tried to do a minimum of two full revisions
before the exam.</p>

<p>Exam days were off days, I tried not to study before the exam. I would just
get more nervous, and I don’t think it pays off a lot if you studied before
this point.</p>

<h2 id="other-topics">Other topics</h2>

<p>In this section I will go over some disconnected topics that are often
discussed in the online study community. I leave them here because most of them
aren’t really critical, and doing what works best for you or makes your life
more enjoyable is probably the right choice.</p>

<h4 id="note-taking">Note taking</h4>

<p>Many people take notes during classes. Of course, if your professor doesn’t
provide any material, you are basically forced to. In other cases, I prefer not
taking notes and focusing on understanding everything. If taking notes helps
you keep your focus that’s okay, but I wouldn’t sacrifice understanding what is
being said to take my own notes. If you are able to keep up with the class and
still take notes, you could try creating a concept map during class, trying to
connect ideas and getting some higher order thinking done.</p>

<p>Cleaning up notes is more often than not very useless. It is very passive. It
might make people feel productive but it is not forcing them to think about the
topic. Summarizing can be a bit more active, since you need to evaluate and
rank the information in terms of importance. If you can add mindmapping to
connect ideas and get a high level picture of the topics it’s probably even
better. In any case, I think it is better to spend your time solving problems,
and would only do these if I have spare time.</p>

<h4 id="study-block-sizes">Study block sizes</h4>

<p>How long do you study and rest for will depend on your preferences. I like
using the Pomodoro method (with 25 minute sessions and 5 minute rests) for
activities that are not very engaging, such us studying theory, or when I am
having trouble focusing. For problem solving or coding I like going with the
flow and taking rests whenever I feel very tired, typically every hour and a
half or two hours.</p>

<h4 id="scheduling-study-sessions">Scheduling study sessions</h4>

<p>When scheduling my sessions and activities, I try to get the least engaging or
most boring things out of the way first, when I am not tired. Even though these
things are often less important, I have no trouble keeping my focus for long
hours on engaging activities like problem solving or coding. I also try to
keep the study time (without breaks) below 7-8 hours each day, and I will
almost never study after dinner. That’s very personal, some people might prefer
studying fewer days a week and more hours, or studying during the night.</p>

<h2 id="conclusions">Conclusions</h2>

<p>That’s it for my studying advice. Not many novel ideas, just focusing on what’s
important and allocating a lot of time. Of course, you will need some balance
to make studying sustainable. I did all of this while going to the gym and
spending time with friends regularly. I wish you good luck with your studies
and a good time during your university years :)</p>]]></content><author><name>Pablo Miralles</name></author><category term="studying" /><summary type="html"><![CDATA[In this post I reflect on my process during my bachelor studies. I try to synthesize what worked for doing good on my courses, both in terms of learning and grades.]]></summary></entry></feed>